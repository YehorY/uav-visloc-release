"""
experiments/ — LEVEL 2.3 ablation code, quarantined from the production pipeline.

Nothing here runs unless the `experiments:` config section enables it. The default configuration
is proven cell-identical to the Level 2 reference frame log (sha256 63e3e4bb...).
"""
